# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/disaster_tracker

# Server Configuration
NODE_ENV=development
PORT=5000

# Optional: If you want to use MongoDB Atlas (comment out the local URI above)
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/disaster_tracker?retryWrites=true&w=majority
